{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Kitbench: asset protocol for webview audio decode (SPEC §8).",
  "windows": ["main"],
  "permissions": ["core:default", "drag:default"]
}
