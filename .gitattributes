# Windows PowerShell 5.1 decodes .ps1 as the ANSI code page unless the file has
# a BOM, so these must stay CRLF and must not be "normalised" on checkout.
*.ps1 text eol=crlf working-tree-encoding=UTF-8
