{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Phase 0 spike: drag-out plus the asset protocol needed by the decode bench.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "drag:default"
  ]
}
